version: '3.8'

services:
  pdf-service:
    build: .
    container_name: react-pdf-service
    ports:
      - "8889:8889"
    environment:
      - PORT=8889
      - NODE_ENV=production
    restart: unless-stopped
